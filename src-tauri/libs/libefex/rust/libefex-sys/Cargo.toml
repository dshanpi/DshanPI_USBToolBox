[package]
name = "libefex-sys"
version = "0.1.0"
authors = ["DshanPI"]
description = "FFI bindings for libefex"
license = "MIT"
edition = "2021"
links = "efex"

[lib]
name = "libefex_sys"

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1.0"
cmake = "0.1"
pkg-config = "0.3"
