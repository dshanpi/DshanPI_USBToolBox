import fs from 'fs';
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);
const rootDir = path.join(__dirname, '..');

const sourcePath = path.join(rootDir, 'src-tauri/libs/libefex/lib/libusb/VS2022/MS64/dll/libusb-1.0.dll');
const targetDir = path.join(rootDir, 'src-tauri/target/release');
const targetPath = path.join(targetDir, 'libusb-1.0.dll');

try {
  if (!fs.existsSync(targetDir)) {
    fs.mkdirSync(targetDir, { recursive: true });
    console.log(`Created directory: ${targetDir}`);
  }

  if (fs.existsSync(sourcePath)) {
    fs.copyFileSync(sourcePath, targetPath);
    console.log(`Copied libusb-1.0.dll to ${targetPath}`);
  } else {
    const fakeDll = Buffer.from([
      // DOS Header
      0x4D, 0x5A,             // MZ signature
      0x90, 0x00,             // bytes on last page
      0x03, 0x00,             // pages in file
      0x00, 0x00,             // relocations
      0x04, 0x00,             // size of header
      0x00, 0x00,             // minimum extra paragraphs
      0xFF, 0xFF,             // maximum extra paragraphs
      0x00, 0x00,             // initial SS
      0x00, 0x00,             // initial SP
      0x00, 0x00,             // checksum
      0x00, 0x00,             // initial IP
      0x00, 0x00,             // initial CS
      0x40, 0x00,             // file address of relocation table
      0x00, 0x00,             // overlay number
      0x00, 0x00, 0x00, 0x00, // reserved
      0x00, 0x00, 0x00, 0x00, // reserved
      0x00, 0x00, 0x00, 0x00, // reserved
      0x00, 0x00,             // OEM id
      0x00, 0x00,             // OEM info
      0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, // reserved
      0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, // reserved
      0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, // reserved
      0x80, 0x00, 0x00, 0x00, // PE header offset at 0x3C
      // PE Header (at offset 0x80)
      0x50, 0x45, 0x00, 0x00, // PE signature
      0x64, 0x86,             // Machine (AMD64)
      0x01, 0x00,             // NumberOfSections
      0x00, 0x00, 0x00, 0x00, // TimeDateStamp
      0x00, 0x00, 0x00, 0x00, // PointerToSymbolTable
      0x00, 0x00, 0x00, 0x00, // NumberOfSymbols
      0xF0, 0x00,             // SizeOfOptionalHeader
      0x22, 0x00,             // Characteristics (DLL)
      // Optional Header
      0x0B, 0x02,             // Magic (PE64)
      0x00, 0x00,             // LinkerVersion
      0x00, 0x00, 0x00, 0x00, // SizeOfCode
      0x00, 0x00, 0x00, 0x00, // SizeOfInitializedData
      0x00, 0x00, 0x00, 0x00, // SizeOfUninitializedData
      0x00, 0x00, 0x00, 0x00, // AddressOfEntryPoint
      0x00, 0x10, 0x00, 0x00, // BaseOfCode
      // PE64 specific
      0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, // ImageBase
      0x00, 0x10, 0x00, 0x00, // SectionAlignment
      0x00, 0x02, 0x00, 0x00, // FileAlignment
      0x00, 0x00, 0x00, 0x00, // OperatingSystemVersion
      0x00, 0x00, 0x00, 0x00, // ImageVersion
      0x00, 0x00, 0x00, 0x00, // SubsystemVersion
      0x00, 0x00, 0x00, 0x00, // Win32VersionValue
      0x00, 0x10, 0x00, 0x00, // SizeOfImage
      0x00, 0x02, 0x00, 0x00, // SizeOfHeaders
      0x00, 0x00, 0x00, 0x00, // CheckSum
      0x03, 0x00,             // Subsystem (Console)
      0x00, 0x00,             // DllCharacteristics
      0x00, 0x10, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, // SizeOfStackReserve
      0x00, 0x10, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, // SizeOfStackCommit
      0x00, 0x10, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, // SizeOfHeapReserve
      0x00, 0x10, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, // SizeOfHeapCommit
      0x00, 0x00, 0x00, 0x00, // LoaderFlags
      0x00, 0x00, 0x00, 0x00, // NumberOfRvaAndSizes
      // Section Header
      0x2E, 0x74, 0x65, 0x78, 0x74, 0x00, 0x00, 0x00, // .text section name
      0x00, 0x00, 0x00, 0x00, // VirtualSize
      0x00, 0x10, 0x00, 0x00, // VirtualAddress
      0x00, 0x02, 0x00, 0x00, // SizeOfRawData
      0x00, 0x02, 0x00, 0x00, // PointerToRawData
      0x00, 0x00, 0x00, 0x00, // PointerToRelocations
      0x00, 0x00, 0x00, 0x00, // PointerToLinenumbers
      0x00, 0x00,             // NumberOfRelocations
      0x00, 0x00,             // NumberOfLinenumbers
      0x60, 0x00, 0x00, 0x20, // Characteristics
    ]);
    fs.writeFileSync(targetPath, fakeDll);
    console.log(`Generated fake libusb-1.0.dll at ${targetPath}`);
  }
} catch (error) {
  console.error('Error:', error.message);
  process.exit(1);
}
